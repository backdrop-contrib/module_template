type = module
name = Foo Module
description = Template project for a Backdrop module
backdrop = 1.x

;Add configure when the module has an administration page.
configure = admin/config/system/foo

;Add package when the module belongs with a group of other modules like `Views`.
;package = Spam control
